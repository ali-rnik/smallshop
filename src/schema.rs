table! {
    products (product_id) {
        product_id -> Nullable<Integer>,
        product_name -> Text,
        number_weight -> Text,
        supplier -> Text,
        produce_date -> Text,
        expire_date -> Text,
        address -> Text,
        unit_price -> Text,
    }
}

